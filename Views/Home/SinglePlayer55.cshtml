@{
    ViewData["Title"] = "Home Page";
}

@model MainModel

<div class="text-center">
    <h1 class="display-4">Welcome to XO by mat7400 - sinleplayer mode 5x5 </h1>
    <!-- поле 5х5 -->
    <p>
        >Click  to generate X or O</p>
        <p> 
        <a asp-action="reload">reload game</a>
        <!-- вставить верную ссылку в УРЛ  -->
        <div style="background-image:url(@Url.Content("https://cdn-icons.flaticon.com/png/512/1078/premium/1078469.png"))"  >>>>>>>>>></div> 
    </p>
    <p>player @Model.CurrentPlayer move</p>
    @for (int i = 0; i < 5; i++)
    {
        @Html.Raw(Model.beginhtml[i]); 
        for (int j = 0; j < 5; j++)
        {
            string curr = Model.strarr[j,i];

            @Html.Raw(curr); 
            
            @Model.Field2[j,i];

            @Html.Raw("  </a></div>");
        }
        @Html.Raw(Model.endhtml[i]);  
            
    }

    
    
</div>
